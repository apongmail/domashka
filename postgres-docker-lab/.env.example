# Скопіюйте у .env і заповніть. Файл .env не комітиться.
POSTGRES_PASSWORD=change-me
# Каталог macOS для bind mount (варіант BIND), буде створений скриптом.
PG_LAB_BIND_DIR=/Users/youruser/pg-lab-data/bind-pgdata
